<UserControl x:Class="dot_net_playground.Views.ExceptionExampleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:dot_net_playground.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <StackPanel Orientation="Vertical">
        <StackPanel Orientation="Horizontal">
            <Button Content="Array out of bounds error" Command="{Binding GetArrayOutOfBoundsExceptionCommand}"/>
        </StackPanel>

        <ContentControl  Content="{Binding CurrentProduct}" />

        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Fetch Time" VerticalAlignment="Center" />
            <TextBox Text="{Binding FetchTime}" />
        </StackPanel>
    </StackPanel>
</UserControl>
